# Runtime data (not source)
assignments.json
uploads/
server.log
tunnel.log
tunnel-url.txt
tmv.db
tmv.db-shm
tmv.db-wal
server.pid
watchdog.lock
watchdog.log

# Node
node_modules/
npm-debug.log*

# Secrets (NEVER commit — contains live API keys)
.env

# OS / editor
.DS_Store
*.swp
tunnel.pid
watchdog.boot.log
